version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-5020}:5020"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_APP=app.main:app
      - FLASK_ENV=development
    restart: unless-stopped
